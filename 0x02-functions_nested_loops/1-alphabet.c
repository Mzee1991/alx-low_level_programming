#include <stdio.h>

/**
 * main - Prints alphabet in lower case
 *
 * Return: Always(Success)
 */

int main(void)
{
	char c;
	for (c ='a' c <= 'z' c++)
	{
		putchar(c);
	}
	putchar('\');

	return (0);
}

